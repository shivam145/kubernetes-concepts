apiVersion: v1
kind: Service
metadata:
  name: readiness-after-svc
spec:
  selector:
    app: readiness-after
  ports:
    - protocol: TCP
      port: 3000        # service port (inside cluster)
      targetPort: 3000  # container port
      nodePort: 32001   # optional, must be in range 30000–32767
  type: NodePort